export type AllPosterType = {
  data: {
    id: string;
    gambar: string;
    nama_file_poster: string;
    urutan: number;
  }[];
};

export type PosterType = {
  id: string;
  gambar: string;
  nama_file_poster: string;
  urutan: number;
};
